const firstName = "Alok";

let secondName = "Kumar";

var lastName = "Malik";

// Prefer not to use var
// Because var is function scoped, not block scoped

console.table([firstName, secondName, lastName]);
